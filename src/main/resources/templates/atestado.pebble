<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <title>Atestado Médico</title>
  <style type="text/css">
    @page {
      size: A4;
      margin: 1.5cm;
    }

    body {
      margin: 0;
      padding: 0;
      font-family: Arial, Helvetica, sans-serif;
      font-size: 11pt;
      color: #000;
      line-height: 1.4;
    }

    .page {
      width: 100%;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 20px;
    }

    /* Cabeçalho Superior */
    .header-table td {
      border: none;
      vertical-align: middle;
      padding: 10px;
    }

    .logo-cell {
      width: 120px;
      text-align: center;
    }

    .logo-cell img {
      max-width: 110px;
      max-height: 70px;
    }

    /* Marca d'água */
    .watermark {
      position: fixed;
      top: 28%;
      left: 10%;
      width: 80%;
    }
    .watermark img { width: 100%; height: auto; }

    .clinic-name-header {
      text-align: center;
      font-size: 14pt;
      font-weight: normal;
    }

    .doc-title {
      text-align: center;
      font-size: 13pt;
      font-weight: bold;
      text-transform: uppercase;
      margin: 20px 0;
    }

    /* Tabelas de conteúdo */
    .content-table td {
      border: 1px solid #000;
      padding: 8px 12px;
    }

    .th-gray {
      background-color: #e0e0e0;
      font-weight: bold;
    }

    /* Corpo do atestado */
    .atestado-body {
      vertical-align: top;
      padding: 20px 12px;
      font-size: 12pt;
      line-height: 2.0;
      text-align: justify;
      min-height: 120px;
    }

    .motivo-body {
      vertical-align: top;
      white-space: pre-wrap;
      min-height: 80px;
    }

    /* Rodapé / Assinatura */
    .signature-container {
      margin-top: 50px;
      width: 100%;
    }

    .signature-wrapper {
      float: right;
      width: 320px;
      text-align: center;
    }

    .signature-line {
      border-top: 1px solid #000;
      margin-bottom: 5px;
    }

    .signature-info {
      font-size: 10pt;
      color: #666;
      font-weight: bold;
    }

    .specialty-info {
      font-size: 9pt;
      color: #999;
    }

    .clearfix:after {
      content: "";
      display: table;
      clear: both;
    }
  </style>
</head>
<body>
  <div class="page">

    {% if clinicWatermarkBase64 is not null %}
    <div class="watermark">
      <img src="{{ clinicWatermarkBase64 }}" alt="" />
    </div>
    {% endif %}

    <table class="header-table">
      <tr>
        <td class="logo-cell">
          {% if clinicLogoBase64 is not null %}
            <img src="{{ clinicLogoBase64 }}" width="100" height="60" alt="Logo {{ clinicName }}" />
          {% endif %}
        </td>
        <td class="clinic-name-header">{{ clinicName }}</td>
        <td class="logo-cell">
          {% if systemLogoBase64 is not null %}
            <img src="{{ systemLogoBase64 }}" width="100" height="60" alt="Solutions Clinic" />
          {% endif %}
        </td>
      </tr>
    </table>

    <div class="doc-title">Atestado Médico</div>

    <table class="content-table">
      <tr>
        <td class="th-gray" style="width: 70%;">Atendimento</td>
        <td class="th-gray" style="width: 30%;">Data: {{ date }}</td>
      </tr>
      <tr>
        <td colspan="2">
          <span>Paciente:</span> {{ patientName }}{% if cpf %} &#160;|&#160; <span>CPF:</span> {{ cpf }}{% endif %}
        </td>
      </tr>
    </table>

    <table class="content-table">
      <tr>
        <td class="th-gray">Declaração</td>
      </tr>
      <tr>
        <td class="atestado-body">
          Atesto que <strong>{{ patientName }}</strong>{% if cpf %}, portador(a) do CPF <strong>{{ cpf }}</strong>,{% endif %}
          esteve sob meus cuidados clínicos e necessita de afastamento de suas atividades pelo prazo de
          <strong>{{ dias }} dia(s)</strong>, a partir de <strong>{{ date }}</strong>.
        </td>
      </tr>
    </table>

    {% if motivo %}
    <table class="content-table">
      <tr>
        <td class="th-gray">Motivo / CID</td>
      </tr>
      <tr>
        <td class="motivo-body">{{ motivo }}</td>
      </tr>
    </table>
    {% endif %}

    <div class="signature-container clearfix">
      <div class="signature-wrapper">
        <div class="signature-line"></div>
        <div class="signature-info">
          {{ professionalName }} ({{ documentLabel }})
        </div>
        {% if specialty %}
        <div class="specialty-info">
          {{ specialty }}
        </div>
        {% endif %}
      </div>
    </div>

  </div>
</body>
</html>
